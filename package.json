{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev --host",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "optimize-css": "node optimize-css.js",
    "build:full": "nuxt build && npm run optimize-css",
    "generate:full": "nuxt generate && npm run optimize-css"
  },
  "dependencies": {
    "@nuxt/types": "^2.18.1",
    "@pinia/nuxt": "^0.5.4",
    "nuxt": "^3.12.4",
    "pinia": "^2.2.2",
    "vue": "latest",
    "vue-i18n": "^9.14.0"
  },
  "devDependencies": {
    "@nuxtjs/i18n": "^8.5.0",
    "nuxt-beastcss": "^1.0.3",
    "sass": "^1.77.8",
    "sass-loader": "^16.0.1",
    "vite": "^5.4.2"
  }
}
